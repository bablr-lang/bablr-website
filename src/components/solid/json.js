export const defaultJSONGrammar =
  "/* global helpers */\nconst sym = helpers.symbols;\nconst { buildBoolean, buildNullTag, buildString, buildIdentifier } =\n  helpers.builders;\nconst { isObject } = helpers.object;\nconst { freeze } = Object;\nconst { EmbeddedObject } = helpers.symbols;\nconst productions = helpers.productions;\nconst { o, eat, eatMatch, match, fail, defineAttribute } = helpers.grammar;\nconst { spam: m, re } = helpers.shorthand;\n\nconst canonicalURL = \"https://bablr.org/languages/core/en/json\";\n\nfunction* eatMatchTrivia() {\n  if (yield match(re`/[ \\t\\r\\n]/`)) {\n    return yield eat(m`#: <*Space />`);\n  }\n  return null;\n}\n\nfunction first(iter) {\n  for (let value of iter) return value;\n}\n\nconst grammar = class JSONGrammar {\n  constructor() {}\n\n  *[Symbol.for(\"@bablr/fragment\")]({ props: { rootMatcher } }) {\n    yield* eatMatchTrivia();\n    yield eat(rootMatcher);\n    yield* eatMatchTrivia();\n  }\n  *Expression() {\n    yield eat(m`<_Any />`, [\n      m`<Array '[' />`,\n      m`<Object '{' />`,\n      m`<String /['\"]/ />`,\n      m`<Number /\\d|-[\\d\\g]/ {span: 'Number'} />`,\n      m`<Infinity /-?Infinity/ />`,\n      m`<Null 'null' />`,\n      m`<Boolean /true|false/ />`,\n    ]);\n  }\n  *Array() {\n    yield eat(m`openToken*: <* '[' { balanced: ']' } />`);\n    yield eat(\n      m`<__List />`,\n      o({\n        element: m`elements[]+: <_Expression />`,\n        separator: m`#separatorTokens: <* ',' />`,\n        allowTrailingSeparator: false,\n      }),\n    );\n    yield eat(m`closeToken*: <* ']' { balancer: true } />`);\n  }\n  *Object() {\n    yield eat(m`openToken*: <* '{' { balanced: '}' } />`);\n    let sep = true;\n    yield eatMatch(m`#separatorTokens: []`);\n    yield eatMatch(m`properties[]: []`);\n    while (sep && (yield match(re`/.|\\g/s`))) {\n      let suppressGap = !!(yield match(m`<__All />`, [\n        m`key: <//>`,\n        m`sigilToken: <* ':' />`,\n      ]));\n      yield eat(\n        m`properties[]: <Property />`,\n        null,\n        o({\n          suppressGap,\n        }),\n      );\n      sep = yield eatMatch(m`#separatorTokens: <* ',' />`);\n    }\n    yield eat(m`closeToken*: <* '}' { balancer: true } />`);\n  }\n  *Property() {\n    yield eat(m`key: <String />`);\n    yield eat(m`sigilToken: <* ':' />`);\n    yield eat(m`value+: <_Expression />`);\n  }\n  *String() {\n    yield eat(\n      m`openToken*: <* '\"' { balanced: '\"', balancedSpan: 'String:Double' } />`,\n    );\n    yield eat(m`content: <*StringContent />`);\n    yield eat(m`closeToken*: <* '\"' { balancer: true } />`);\n  }\n  *StringContent({ state: { span } }) {\n    let esc, lit;\n    do {\n      esc = (yield match(\"\\\\\")) && (yield eat(m`@: <EscapeSequence />`));\n      lit =\n        span === \"String:Single\"\n          ? yield eatMatch(re`/[^\\r\\n\\\\'\\g]+/`)\n          : yield eatMatch(re`/[^\\r\\n\\\\\"\\g]+/`);\n    } while (esc || lit);\n  }\n  *EscapeSequence({ state: { span }, ctx }) {\n    if (!span.startsWith(\"String\")) {\n      yield fail();\n    }\n    yield eat(m`sigilToken: <* '\\\\' { openSpan: 'Escape' } />`);\n    let match_;\n    let cooked;\n    if (\n      (match_ =\n        span === \"String:Single\"\n          ? yield match(re`/[\\\\/nrt0']/`)\n          : yield match(re`/[\\\\/nrt0\"]/`))\n    ) {\n      const matchText = ctx.sourceTextFor(match_);\n      yield eat(\n        m`code: <*Keyword ${buildString(matchText)} { closeSpan: 'Escape' } />`,\n      );\n      cooked = escapables.get(matchText) || matchText;\n    } else if (yield match(\"u\")) {\n      let codeNode = yield eat(m`code: <EscapeCode { closeSpan: 'Escape' } />`);\n      if (codeNode && first(codeNode.children)?.value.flags.token) {\n        const match_ = ctx.sourceTextFor(codeNode);\n      } else {\n        const type = ctx.sourceTextFor(codeNode.get(\"typeToken\"));\n        if (type) {\n          const value = ctx.sourceTextFor(codeNode.get(\"value\"));\n          if (!span.startsWith(\"String\")) {\n            throw new Error(\"not implemented\");\n          }\n          if (type === \"u\") {\n            cooked = String.fromCharCode(parseInt(value, 16));\n          } else {\n            throw new Error();\n          }\n        } else {\n          let value = ctx.sourceTextFor(codeNode);\n          cooked = escapables.get(value) || value;\n        }\n      }\n    } else {\n      yield fail();\n    }\n    yield defineAttribute(\"cooked\", cooked);\n  }\n  *EscapeCode() {\n    if (yield eatMatch(m`typeToken: <*Keyword 'u' />`)) {\n      if (\n        yield eatMatch(\n          m`openToken*: <* '{' { balanced: '}' } />`,\n          null,\n          o({\n            bind: true,\n          }),\n        )\n      ) {\n        yield eat(m`value: <*UnsignedHexInteger />`);\n        yield eat(m`closeToken*: <* '}' { balancer: true } />`);\n      } else {\n        yield eat(m`value: <*UnsignedHexInteger /[\\da-fA-F]{4}/ />`);\n        yield eat(m`closeToken*: null`);\n      }\n    }\n  }\n  *Number() {\n    yield eat(\n      m`wholePart: <Integer />`,\n      o({\n        noDoubleZero: true,\n        matchSign: \"-\",\n      }),\n    );\n    let fs = yield eatMatch(\n      m`fractionalSeparatorToken*: <* '.' />`,\n      null,\n      o({\n        bind: true,\n      }),\n    );\n    if (fs) {\n      yield eat(m`fractionalPart: <*UnsignedInteger />`);\n    } else {\n      yield eat(m`fractionalPart: null`);\n    }\n    let es = yield eatMatch(\n      m`exponentSeparatorToken*: <* /[eE]/ />`,\n      null,\n      o({\n        bind: true,\n      }),\n    );\n    if (es) {\n      yield eat(m`exponentPart: <Integer />`, {\n        matchSign: /[+-]/,\n      });\n    } else {\n      yield eat(m`exponentPart: null`);\n    }\n  }\n  *Integer({ props: { matchSign = null, noDoubleZero = false } }) {\n    if (matchSign) {\n      yield eatMatch(\n        m`signToken: <* ${buildString(matchSign)} />`,\n        null,\n        o({\n          bind: true,\n        }),\n      );\n    } else {\n      yield eat(m`signToken: null`);\n    }\n    yield eat(\n      m`value: <*UnsignedInteger />`,\n      o({\n        noDoubleZero,\n      }),\n    );\n  }\n  *UnsignedInteger({ props: { noDoubleZero = false }, ctx }) {\n    let firstDigit = ctx.sourceTextFor(yield eat(re`/\\d/`));\n    if (!noDoubleZero || firstDigit.value !== \"0\") {\n      yield eatMatch(re`/\\d+/`);\n    }\n  }\n  *UnsignedHexInteger() {\n    yield eatMatch(re`/[\\da-fA-F]+/`);\n  }\n  *Infinity() {\n    yield eatMatch(\n      m`signToken: <* '-' />`,\n      null,\n      o({\n        bind: true,\n      }),\n    );\n    yield eat(m`sigilToken: <*Keyword 'Infinity' />`);\n  }\n  *Boolean() {\n    yield eat(m`sigilToken: <*Keyword /true|false/ />`);\n  }\n  *Null() {\n    yield eat(m`sigilToken: <*Keyword 'null' />`);\n  }\n  *Space() {\n    yield eat(re`/[ \\t\\r\\n]+/`);\n  }\n  *Keyword() {}\n  *List() {}\n  *Any() {}\n  *All() {}\n};\n\nconst escapables = new Map(\n  Object.entries({\n    b: \"\\b\",\n    // these two escapes are antiquated\n    f: \"\\f\",\n    // but giving their meaning away could be confusing\n    n: \"\\n\",\n    r: \"\\r\",\n    t: \"\\t\",\n    0: \"\\0\",\n  }),\n);";
